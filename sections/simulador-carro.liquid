{%- liquid
  comment
    El widget se renderiza cuando el carrito tiene al menos un
    dispositivo elegible: iPhone, iPad, Mac Desktop (iMac / Mac mini /
    Mac Studio / Mac Pro) o Mac Notebook (MacBook Air / MacBook Pro).
    En ese caso TODO el carrito entra al arriendo: los dispositivos
    con su factor propio y los accesorios con la tasa 'Otros'.
    Si ningún producto es elegible, el widget no se renderiza.
  endcomment
  assign qrc_hay_arriendo = false
  for item in cart.items
    assign qrc_pt = item.product.type | default: '' | strip
    assign qrc_p4 = qrc_pt | slice: 0, 4
    assign qrc_p6 = qrc_pt | slice: 0, 6
    assign qrc_p7 = qrc_pt | slice: 0, 7
    if qrc_p6 == 'iPhone' or qrc_p4 == 'iPad' or qrc_p7 == 'MacBook' or qrc_p4 == 'iMac' or qrc_pt == 'Mac mini' or qrc_pt == 'Mac Studio' or qrc_pt == 'Mac Pro'
      assign qrc_hay_arriendo = true
      break
    endif
  endfor
-%}
{%- style -%}
.qrc {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  -webkit-font-smoothing: antialiased;
  background: #fff;
  border-radius: 16px;
  border: 1px solid #e5e5ea;
  padding: 20px;
  margin: 16px 0;
  color: #1d1d1f;
}
.qrc *, .qrc *::before, .qrc *::after { box-sizing: border-box; margin: 0; padding: 0; }

/* — Header — */
.qrc-header { margin-bottom: 16px; }
.qrc-badge { display: inline-block; font-size: 10px; font-weight: 600; text-transform: uppercase; letter-spacing: 0.06em; color: #fff; background: #0071e3; padding: 2px 8px; border-radius: 100px; margin-bottom: 8px; }
.qrc-title { font-size: 17px; font-weight: 600; letter-spacing: -0.02em; color: #1d1d1f; }
.qrc-subtitle { font-size: 13px; color: #6e6e73; margin-top: 2px; }

/* — Plazo row — */
.qrc-options { display: flex; flex-wrap: wrap; gap: 12px; align-items: center; margin-bottom: 16px; }
.qrc-plazo-group { display: flex; gap: 6px; }
.qrc-plazo { padding: 8px 14px; border: 1.5px solid #d2d2d7; border-radius: 10px; background: #fff; cursor: pointer; font-family: inherit; text-align: center; transition: all 0.2s; font-size: 13px; font-weight: 600; color: #1d1d1f; }
.qrc-plazo[aria-pressed="true"] { border-color: #1d1d1f; background: #1d1d1f; color: #fff; }

/* — Line items — */
.qrc-sep { height: 1px; background: #e5e5ea; margin: 12px 0; }
.qrc-items { list-style: none; }
.qrc-item { display: flex; align-items: center; gap: 12px; padding: 10px 0; }
.qrc-item + .qrc-item { border-top: 1px solid #f0f0f0; }
.qrc-item-img { width: 48px; height: 48px; border-radius: 8px; background: #f5f5f7; overflow: hidden; flex-shrink: 0; display: flex; align-items: center; justify-content: center; }
.qrc-item-img img { width: 100%; height: 100%; object-fit: contain; }
.qrc-item-info { flex: 1; min-width: 0; }
.qrc-item-name { font-size: 13px; font-weight: 600; color: #1d1d1f; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.qrc-item-detail { font-size: 11px; color: #6e6e73; margin-top: 1px; }
.qrc-item-type { display: inline-block; font-size: 10px; font-weight: 500; color: #0071e3; background: rgba(0,113,227,0.08); padding: 1px 6px; border-radius: 4px; }
.qrc-item-type--venta { color: #b25000; background: rgba(178,80,0,0.08); }
.qrc-item-cuota { text-align: right; flex-shrink: 0; }
.qrc-item-cuota-val { font-size: 15px; font-weight: 600; color: #1d1d1f; letter-spacing: -0.02em; }
.qrc-item-cuota-sub { font-size: 11px; color: #6e6e73; }

/* — Total — */
.qrc-total { display: flex; align-items: baseline; justify-content: space-between; padding: 14px 0 0; }
.qrc-total-label { font-size: 13px; font-weight: 600; color: #1d1d1f; text-transform: uppercase; letter-spacing: 0.04em; }
.qrc-total-val { font-size: 24px; font-weight: 600; letter-spacing: -0.04em; color: #1d1d1f; }
.qrc-total-val sup { font-size: 14px; font-weight: 500; vertical-align: top; margin-top: 2px; display: inline-block; }
.qrc-total-suffix { font-size: 14px; font-weight: 400; color: #6e6e73; }
.qrc-total-meta { text-align: right; }
.qrc-total-iva { font-size: 13px; color: #757575; font-weight: 600; }
.qrc-total-plazo { font-size: 11px; color: #6e6e73; margin-top: 2px; }

/* CTA removed — cotizador handles quoting */

/* — Empty state — */
.qrc-empty { text-align: center; padding: 20px 0; color: #6e6e73; font-size: 14px; }

/* — Animation — */
@keyframes qrcPop { from { opacity: 0.25; transform: translateY(2px); } to { opacity: 1; transform: none; } }
.qrc-pop { animation: qrcPop 0.2s ease-out both; }

/* — Responsive — */
@media (max-width: 480px) {
  .qrc-options { flex-direction: column; align-items: stretch; }
  .qrc-total-val { font-size: 20px; }
}
{%- endstyle -%}

{%- if cart.item_count > 0 and qrc_hay_arriendo -%}
{% render 'arriendo-config' %}
<div class="qrc" id="qrc-widget">
  <div class="qrc-header">
    <div class="qrc-badge">Arriendo Operativo</div>
    <div class="qrc-title">Arrienda tu carrito completo</div>
    <div class="qrc-subtitle">Cuotas mensuales estimadas para todo tu carrito</div>
  </div>

  <!-- Plazo -->
  <div class="qrc-options">
    <div class="qrc-plazo-group" role="group" aria-label="Plazo del arriendo">
      <button class="qrc-plazo" id="qrc-p12" aria-pressed="false">12 meses</button>
      <button class="qrc-plazo" id="qrc-p24" aria-pressed="false">24 meses</button>
      <button class="qrc-plazo" id="qrc-p36" aria-pressed="true">36 meses</button>
    </div>
  </div>

  <div class="qrc-sep"></div>

  <!-- Line items -->
  <ul class="qrc-items" id="qrc-items">
    {%- for item in cart.items -%}
    {%- liquid
      assign qrc_pt = item.product.type | default: '' | strip
      assign qrc_p4 = qrc_pt | slice: 0, 4
      assign qrc_p6 = qrc_pt | slice: 0, 6
      assign qrc_p7 = qrc_pt | slice: 0, 7
      assign qrc_item_tipo = ''
      if qrc_p6 == 'iPhone'
        assign qrc_item_tipo = 'iPhone'
      elsif qrc_p4 == 'iPad'
        assign qrc_item_tipo = 'iPad'
      elsif qrc_p7 == 'MacBook'
        assign qrc_item_tipo = 'Mac Notebook'
      elsif qrc_p4 == 'iMac' or qrc_pt == 'Mac mini' or qrc_pt == 'Mac Studio' or qrc_pt == 'Mac Pro'
        assign qrc_item_tipo = 'Mac Desktop'
      endif
    -%}
    <li class="qrc-item" data-qrc-index="{{ forloop.index0 }}">
      <div class="qrc-item-img">
        {%- if item.image -%}
          <img src="{{ item.image | image_url: width: 96 }}" alt="{{ item.product.title | escape }}" loading="lazy">
        {%- endif -%}
      </div>
      <div class="qrc-item-info">
        <div class="qrc-item-name">{{ item.product.title }}</div>
        <div class="qrc-item-detail">
          {%- if qrc_item_tipo != blank -%}
            <span class="qrc-item-type" id="qrc-type-{{ forloop.index0 }}">{{ qrc_item_tipo }}</span>
            · {{ item.quantity }} {{ item.quantity | pluralize: 'equipo', 'equipos' }}
          {%- else -%}
            <span class="qrc-item-type" id="qrc-type-{{ forloop.index0 }}">Accesorio</span>
            · {{ item.quantity }} {{ item.quantity | pluralize: 'unidad', 'unidades' }}
          {%- endif -%}
        </div>
      </div>
      <div class="qrc-item-cuota">
        <div class="qrc-item-cuota-val" id="qrc-cuota-{{ forloop.index0 }}">$-</div>
        <div class="qrc-item-cuota-sub">/mes sin IVA</div>
      </div>
    </li>
    {%- endfor -%}
  </ul>

  <div class="qrc-sep"></div>

  <!-- Total -->
  <div class="qrc-total">
    <div class="qrc-total-label">Cuota total mensual</div>
    <div class="qrc-total-meta">
      <div class="qrc-total-val"><sup>$</sup><span id="qrc-total">-</span><span class="qrc-total-suffix">/mes</span></div>
      <div class="qrc-total-iva" id="qrc-total-iva">Con IVA: $-/mes</div>
      <div class="qrc-total-plazo" id="qrc-total-plazo">36 meses · sin IVA</div>
    </div>
  </div>


</div>
{%- endif -%}

{%- if cart.item_count > 0 and qrc_hay_arriendo -%}
<script>
(function() {
  'use strict';

  /* --- Cart items data from Liquid --- */
  var cartItems = [
    {%- for item in cart.items -%}
    {
      price: {{ item.price }} / 100,
      qty: {{ item.quantity }},
      ptype: {{ item.product.type | default: '' | json }},
      title: {{ item.product.title | json }}
    }{%- unless forloop.last -%},{%- endunless -%}
    {%- endfor -%}
  ];

  /* Dispositivos elegibles: iPhone, iPad, Mac Desktop y Mac
     Notebook. El resto se arrienda con la tasa 'Otros' cuando
     acompaña a un dispositivo (condición de render del widget). */
  function clasificarArriendo(productType) {
    var t = (productType || '').trim();
    if (t.indexOf('iPhone') === 0) return 'iPhone';
    if (t.indexOf('iPad') === 0) return 'iPad';
    if (t.indexOf('MacBook') === 0) return 'Mac Notebook';
    if (t.indexOf('iMac') === 0 || t === 'Mac mini' || t === 'Mac Studio' || t === 'Mac Pro') return 'Mac Desktop';
    return null;
  }

  /* Assign types */
  for (var i = 0; i < cartItems.length; i++) {
    cartItems[i].type = clasificarArriendo(cartItems[i].ptype);
    cartItems[i].esDispositivo = cartItems[i].type !== null;
  }

  /* Parámetros centralizados (factores por tipo, IVA) */
  var CFG = window.ArriendoConfig;
  var cMeses = CFG.PLAZO_DEFAULT;

  function fmtMiles(n) { return Math.round(n).toString().replace(/\B(?=(\d{3})+(?!\d))/g, '.'); }

  /* Plazo */
  CFG.PLAZOS.forEach(function(p) {
    var btn = document.getElementById('qrc-p' + p);
    if (btn) btn.addEventListener('click', function() { setCMeses(p); });
  });
  function setCMeses(m) {
    cMeses = m;
    CFG.PLAZOS.forEach(function(p) {
      var btn = document.getElementById('qrc-p' + p);
      if (btn) btn.setAttribute('aria-pressed', m === p);
    });
    cCalcular();
  }

  /* Animation helper */
  function cSet(id, val) {
    var el = document.getElementById(id);
    if (!el) return;
    el.classList.remove('qrc-pop');
    void el.offsetWidth;
    el.textContent = val;
    el.classList.add('qrc-pop');
  }

  /* Main calculation — dispositivos con su factor, accesorios con
     tasa 'Otros'. */
  function cCalcular() {
    var totalCuota = 0;
    var totalEquipos = 0;

    for (var i = 0; i < cartItems.length; i++) {
      var item = cartItems[i];

      var tipo = item.esDispositivo ? item.type : 'Otros';
      var cuotaLinea = CFG.cuotaNeta(item.price * item.qty, cMeses, tipo);

      cSet('qrc-cuota-' + i, '$' + fmtMiles(cuotaLinea));
      totalCuota += cuotaLinea;
      if (item.esDispositivo) totalEquipos += item.qty;
    }

    cSet('qrc-total', fmtMiles(totalCuota));
    cSet('qrc-total-iva', 'Con IVA: $' + fmtMiles(totalCuota * CFG.IVA) + '/mes');
    document.getElementById('qrc-total-plazo').textContent = cMeses + ' meses · ' + totalEquipos + (totalEquipos === 1 ? ' equipo' : ' equipos') + ' · sin IVA';
  }

  cCalcular();
})();
</script>
{%- endif -%}

{% schema %}
{
  "name": "Arriendo Carrito",
  "settings": [],
  "presets": [
    {
      "name": "Arriendo Carrito",
      "category": "Custom"
    }
  ]
}
{% endschema %}
